spring:
  application:
    name: KOSMEETING
  thymeleaf:
    prefix: classpath:/templates/
  datasource:
    url: jdbc:mysql://localhost:3306/kosmeeting?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: kosmeeting
    password: ${DATABASE_PWD_KOSMEETING}
